{
  "name": "references-python-catalog",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "trigger dev",
    "deploy": "trigger deploy --self-hosted --load-image",
    "install-python-deps": "uv pip sync requirements.txt"
  },
  "dependencies": {
    "@basicblock/trigger-sdk": "workspace:*",
    "@basicblock/trigger-python": "workspace:*",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@basicblock/trigger-build": "workspace:*",
    "@basicblock/trigger-cli": "workspace:*",
    "typescript": "^5.5.4"
  }
}