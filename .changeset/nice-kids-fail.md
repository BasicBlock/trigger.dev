---
"@basicblock/trigger-cli": patch
---

Load environment variables from a mounted env file (`TRIGGER_MOUNTED_ENV_FILE` or `DOPPLER_SECRETS_FILE`) in managed runner entrypoint before starting the controller.
